<UserControl
    x:Class="to_lazy_to_curl.Components.PopupMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:local="clr-namespace:to_lazy_to_curl.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>

        <Popup
            x:Name="PopupMenuWindow"
            AllowsTransparency="True"
            HorizontalOffset="-150"
            Placement="Bottom"
            PlacementTarget="{Binding ElementName=ThemeIcon}"
            PopupAnimation="Fade"
            StaysOpen="True"
            VerticalOffset="6">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Arrow  -->
                <Polygon
                    Grid.Row="0"
                    Margin="0,0,16,0"
                    HorizontalAlignment="Right"
                    Panel.ZIndex="1"
                    Fill="{DynamicResource UrlInputBackground}"
                    Points="0,10 10,0 20,10"
                    Stroke="{DynamicResource UrlInputBackground}"
                    StrokeThickness="1" />

                <!--  Box  -->
                <Border
                    Grid.Row="1"
                    Width="200"
                    Height="100"
                    Padding="10"
                    Panel.ZIndex="0"
                    Background="{DynamicResource UrlInputBackground}"
                    CornerRadius="6">
                    <StackPanel>
                        <TextBlock Margin="0,4" Text="Light Theme" />
                        <TextBlock Margin="0,4" Text="Dark Theme" />
                        <TextBlock Margin="0,4" Text="System Default" />
                    </StackPanel>
                </Border>
            </Grid>
        </Popup>

    </Grid>

</UserControl>
