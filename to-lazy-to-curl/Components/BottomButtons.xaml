<UserControl
    x:Class="to_lazy_to_curl.Components.BottomButtons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:local="clr-namespace:to_lazy_to_curl.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        Grid.Row="5"
        MaxWidth="900"
        Margin="0,12,0,0">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button
            x:Name="SubmitButton"
            Grid.Column="0"
            Height="42"
            Click="SubmitButton_Click"
            Cursor="Hand"
            FontSize="{DynamicResource FontSizeBottomButtons}"
            FontWeight="Bold">

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <fa:ImageAwesome
                    x:Name="SendButtonStaticIcon"
                    Width="16"
                    Height="16"
                    Margin="0,0,6,0"
                    VerticalAlignment="Center"
                    Foreground="{StaticResource PrimaryText}"
                    Icon="PaperPlaneOutline" />
                <fa:ImageAwesome
                    x:Name="SendButtonSpinnerIcon"
                    Width="16"
                    Height="16"
                    Margin="0,0,6,0"
                    VerticalAlignment="Center"
                    Foreground="{StaticResource PrimaryText}"
                    Icon="Spinner"
                    Spin="True"
                    SpinDuration="2"
                    Visibility="Collapsed" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="{DynamicResource FontSizeBottomButtons}"
                    Text="Submit" />
            </StackPanel>

            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="outer"
                        Background="{StaticResource ButtonBackground}"
                        BorderBrush="{StaticResource Border}"
                        BorderThickness="1"
                        CornerRadius="4">
                        <ContentPresenter
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            RecognizesAccessKey="True"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="outer" Property="BorderBrush" Value="{StaticResource BorderHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="outer" Property="Background" Value="{StaticResource BorderHover}" />
                            <Setter TargetName="outer" Property="BorderBrush" Value="{StaticResource BorderHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button
            x:Name="ClearButton"
            Grid.Column="2"
            Height="42"
            Click="ClearButton_Click"
            Cursor="Hand"
            FontSize="{DynamicResource FontSizeBottomButtons}"
            FontWeight="Bold">

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <fa:ImageAwesome
                    x:Name="ClearButtonIcon"
                    Width="15"
                    Height="15"
                    Margin="0,0,6,0"
                    VerticalAlignment="Center"
                    Foreground="{StaticResource PrimaryText}"
                    Icon="Refresh">
                    <fa:ImageAwesome.RenderTransform>
                        <RotateTransform x:Name="ClearButtonRotate" CenterX="7.5" CenterY="7.5" />
                    </fa:ImageAwesome.RenderTransform>
                </fa:ImageAwesome>
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="{DynamicResource FontSizeBottomButtons}"
                    Text="Reset" />
            </StackPanel>

            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="outer"
                        Background="{StaticResource ButtonBackground}"
                        BorderBrush="{StaticResource Border}"
                        BorderThickness="1"
                        CornerRadius="4">
                        <ContentPresenter
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            RecognizesAccessKey="True"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="outer" Property="BorderBrush" Value="{StaticResource BorderHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="outer" Property="Background" Value="{StaticResource BorderHover}" />
                            <Setter TargetName="outer" Property="BorderBrush" Value="{StaticResource BorderHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
