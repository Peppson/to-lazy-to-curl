<UserControl
    x:Class="to_lazy_to_curl.Components.JsonInput"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:to_lazy_to_curl.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,14,0,6"
            VerticalAlignment="Center"
            FontSize="16"
            FontWeight="Bold"
            Foreground="{StaticResource PrimaryText}"
            Text="Body" />


        <!--  TODO  -->
        <StackPanel
            x:Name="ModeButtonPanel"
            Grid.Row="0"
            Margin="0,1,0,6"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                x:Name="RequestButton"
                Margin="0,0,6,0"
                Click="RequestButton_Click"
                Content="Request" />
            <Button
                x:Name="ResponseButton"
                Click="ResponseButton_Click"
                Content="Response" />
        </StackPanel>


        <Border
            Grid.Row="2"
            BorderThickness="1"
            CornerRadius="4">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="{StaticResource Border}" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource BorderHover}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource BorderHover}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>



            <Grid x:Name="MainGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <avalonEdit:TextEditor Name="JsonTextBox" Grid.Column="0" />
                <avalonEdit:TextEditor Name="ResponseEditor" Grid.Column="2" />
            </Grid>


        </Border>
    </Grid>
</UserControl>




<!--

<UserControl
    x:Class="to_lazy_to_curl.Components.JsonInput"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:to_lazy_to_curl.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,14,0,6"
            VerticalAlignment="Center"
            FontSize="16"
            FontWeight="Bold"
            Foreground="{StaticResource PrimaryText}"
            Text="Body" />

        <StackPanel
            x:Name="ModeButtonPanel"
            Grid.Row="0"
            Margin="0,1,0,6"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                x:Name="RequestButton"
                Margin="0,0,6,0"
                Click="RequestButton_Click"
                Content="Request" />
            <Button
                x:Name="ResponseButton"
                Click="ResponseButton_Click"
                Content="Response" />
        </StackPanel>


        <Border
            Grid.Row="2"
            BorderThickness="1"
            CornerRadius="4">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="{StaticResource Border}" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource BorderHover}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource BorderHover}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <avalonEdit:TextEditor
                Name="JsonTextBox"
                Background="{StaticResource InputBackground}"
                FontSize="14"
                HorizontalScrollBarVisibility="Auto"
                IsReadOnly="false"
                ShowLineNumbers="True"
                SyntaxHighlighting="Json"
                VerticalScrollBarVisibility="Visible" />
        </Border>
    </Grid>
    </UserControl>-->

