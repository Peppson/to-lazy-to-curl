<UserControl
    x:Class="to_lazy_to_curl.Components.HttpButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:local="clr-namespace:to_lazy_to_curl.Components"
    xmlns:local1="clr-namespace:to_lazy_to_curl.Services"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button
        x:Name="MainButton"
        Height="36"
        Content="{Binding ButtonContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
        Cursor="Hand"
        FontSize="{StaticResource FontSizeHttpButtons}"
        FontWeight="Bold">

        <Button.Resources>
            <SolidColorBrush x:Key="PrimaryTextBrush" Color="{DynamicResource PrimaryTextColor}" />
            <SolidColorBrush x:Key="PrimaryTextInverseBrush" Color="{DynamicResource PrimaryTextInverseColor}" />
        </Button.Resources>

        <Button.Template>
            <ControlTemplate TargetType="Button">
                <Border
                    x:Name="outerBorder"
                    Background="{DynamicResource HttpButtonBackground}"
                    BorderBrush="{DynamicResource Border}"
                    BorderThickness="1"
                    ClipToBounds="True"
                    CornerRadius="{StaticResource InnerCornerRadius}">
                    <Grid x:Name="innerGrid">
                        <Border
                            x:Name="bottomBorder"
                            Height="2"
                            VerticalAlignment="Bottom"
                            Background="{Binding ButtonColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CornerRadius="{StaticResource InnerCornerRadius}" />
                        <TextBlock
                            x:Name="baseText"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="{TemplateBinding FontSize}"
                            FontWeight="{TemplateBinding FontWeight}"
                            Foreground="{StaticResource PrimaryTextBrush}"
                            Text="{TemplateBinding Content}" />
                        <TextBlock
                            x:Name="selectedText"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="{TemplateBinding FontSize}"
                            FontWeight="{TemplateBinding FontWeight}"
                            Foreground="{StaticResource PrimaryTextInverseBrush}"
                            IsHitTestVisible="False"
                            Opacity="0"
                            Text="{TemplateBinding Content}" />
                    </Grid>
                </Border>

                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="outerBorder" Property="BorderBrush" Value="{DynamicResource BorderHover}" />
                    </Trigger>

                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="bottomBorder"
                                        Storyboard.TargetProperty="Height"
                                        To="34"
                                        Duration="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <QuadraticEase EasingMode="EaseInOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>

                                    <DoubleAnimation
                                        Storyboard.TargetName="selectedText"
                                        Storyboard.TargetProperty="Opacity"
                                        To="1"
                                        Duration="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <QuadraticEase EasingMode="EaseInOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>

                        <DataTrigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetName="bottomBorder"
                                        Storyboard.TargetProperty="Height"
                                        To="2"
                                        Duration="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <QuadraticEase EasingMode="EaseInOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>

                                    <DoubleAnimation
                                        Storyboard.TargetName="selectedText"
                                        Storyboard.TargetProperty="Opacity"
                                        To="0"
                                        Duration="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <QuadraticEase EasingMode="EaseInOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Button.Template>
    </Button>
</UserControl>
